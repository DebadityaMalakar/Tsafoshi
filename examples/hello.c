#include <stdio.h>

/* The header does nothing: printf is already here. It is accepted because
   real C source has that line at the top, and real C source has to work. */

int main(void)
{
    printf("hello from the north star\n");
    return 0;
}
